#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin>>s;
    sort(s.begin(),s.end());
    // reverse(s.begin(),s.end());->use to reverse a string
    cout<<s;
    return 0;
}
